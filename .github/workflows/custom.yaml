on:
  workflow_dispatch:
  push:
    tags:
      - "**"
      - "!devel"
      - "!devel_latest"
jobs:
  notify_on_slack:
    runs-on: ubuntu-latest
    timeout-minutes: 160
    steps:
      - name: Echo
        run: echo ${{ toJSON(github) }} >> file && cat file