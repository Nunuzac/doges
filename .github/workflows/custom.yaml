on:
  workflow_dispatch:
  pull_request:
    branches:
      - '**'
  push:
    branches:
      - '**'
jobs:
  notify_on_slack:
    runs-on: ubuntu-latest
    timeout-minutes: 160
    steps:
      - run: echo "In"
        if: ${{ contains((github.base_ref || github.ref), 'master') }}
