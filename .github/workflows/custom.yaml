name: awesome_action
on: workflow_dispatch
jobs:
  test:
    runs-on: self-hosted
    steps:
      - name: setup
        run: echo '::set-output name=PAYLOAD::$(echo `PAYLOAD`)'
      - name: print
        run : printenv
      - name: awesome
        uses: Nunuzac/my-action@master