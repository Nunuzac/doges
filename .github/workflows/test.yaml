on:
  workflow_dispatch:
jobs:
  notify_on_slack:
    runs-on: self-hosted
    timeout-minutes: 160
    env:
      LAUNCH_CHECK_EXCLUDES: |
        one
        two
        tree
    steps:
      - uses: actions/checkout@v2
      - name: Tag name
        id: tag_name
        run: ./check.sh doges ${{ env.LAUNCH_CHECK_EXCLUDES }}

